server {
    listen localhost:9000;
    server_name localhost;

    access_log /var/log/nginx/sandhill-access.log;
    error_log /var/log/nginx/sandhill.log warn;

    location / {
        include uwsgi_params;
        uwsgi_pass unix:/var/www/sandhill/sandhill.sock;
    }
}
